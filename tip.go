package main

var tip = `
Hashfiles is a tool for calculate files hash.

Usage:
	hashfiles command [argument]
The commands are:
	-ignorefile, -if
		the ignore files
	-ignoredir, id
		the ignore dir
	-input, -i
		input dir path
	-output, -o
		out file path
	-goroutine, -g
		the number of goroutine
`
